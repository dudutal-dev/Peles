# החלטות והנחות — שלב א' (MVP)

תאריך: 14.9.2026

## תשובות לשאלות הפתוחות (פרק 8 באפיון)

| שאלה | תשובה |
|---|---|
| Local-first או backend? | Local-first בלבד, בלי שרת |
| Google Calendar ב-MVP? | לא, בשלב ב'. ב-MVP יש יומן פנימי פשוט |
| ריבוי מכשירים בשלב א'? | לא (נגזר מהתשובה הראשונה). יש גיבוי ושחזור ידני |
| שם ופלטה | הושאר לבחירה. נבחרו "פלס" ופלטת פטרול/גרפיט, ראו ARD-004 |

---

## ARD-001: PWA מקומי עם IndexedDB (Dexie)
- **הקשר**: המשתמשת בתנועה, לפעמים בלי קליטה; סביבה ארגונית עם מגבלות IT.
- **אפשרויות**: אפליקציה native / PWA עם שרת / PWA מקומי.
- **החלטה**: PWA מקומי. React 19 + TypeScript strict + Vite, נתונים ב-IndexedDB דרך Dexie.
- **נימוק**: אין התקנה מחנות, אין תלות בהרשאות שרת, עובד בלי רשת מהרגע הראשון.
- **השלכות**: אין סנכרון בין מכשירים וראש המינהל לא רואה את הנתונים. הסיכון לאובדן נתונים מטופל בגיבוי JSON, בבקשת אחסון קבוע, ובתזכורת גיבוי במסך ההגדרות.

## ARD-002: service worker שנכתב ידנית במקום Workbox
- **החלטה**: תוסף Vite קטן (`build/serviceWorkerPlugin.ts`) שמייצר `sw.js` עם רשימת כל הקבצים שנבנו.
- **נימוק**: פחות תלויות, שליטה מלאה, והתנהגות צפויה (cache-first לכל הקבצים, גרסה לפי תוכן).
- **השלכות**: נמצא ותוקן באג אמיתי בבדיקה: תגובות עם `Vary: Origin` לא נמצאו במטמון בלי רשת, ולכן נוסף `ignoreVary`.

## ARD-003: ניתוב hash בלי ספריית ניתוב
- **החלטה**: `#/morning`, `#/waiting`, `#/tasks/overdue` וכו'.
- **נימוק**: עובד מכל כתובת אחסון ומתוך תת-תיקייה, בלי הגדרות שרת. כפתור "חזור" בטלפון סוגר גיליון פתוח לפני שהוא מחליף מסך.

## ARD-004: כיוון עיצובי
- **פלטה**: נייר מינרלי קריר `#F1F3F2`, דיו גרפיט `#1A2327`, פטרול `#0E5E6F` לפעולות, אדום `#B3261E` רק לאיחור, צהוב טוש `#FFE45C`.
- **האלמנט הבולט היחיד**: משימה ש"ראש המינהל מחכה לה" מקבלת הדגשת טוש צהוב על הכותרת, כמו סימון ביד, יחד עם תווית טקסט (לא רק צבע).
- **טיפוגרפיה**: IBM Plex Sans Hebrew, מוטמע בחבילה ולכן עובד גם בלי רשת. ספרות ברוחב קבוע לספירות ולתאריכים.
- **נמנעו במכוון**: רקע קרם עם אקצנט טרה-קוטה, כרטיסים זהים עם צל, תוויות ALL-CAPS, "→" בכפתורים. רשימות שטוחות עם קו מפריד במקום ערימת כרטיסים.
- **מצב כהה**: נתמך לפי הגדרת המכשיר.

## ARD-005: "היום" כמפתח תקופה, לא איפוס יזום
- **החלטה**: סימוני שגרה נשמרים לפי מפתח תקופה (`2026-09-14` ליומי, `W2026-09-13` לשבועי). אין תהליך איפוס.
- **נימוק**: עובד נכון גם כשהאפליקציה לא נפתחה כמה ימים, או נשארה פתוחה בלילה.

---

## הנחות שהונחו במהלך הבנייה (לאישור או תיקון)

1. **שבוע עבודה**: ראשון עד חמישי. "סוף השבוע" בקליטה = יום חמישי. פריט שגרה שבועי מתאפס ביום ראשון.
2. **"עד יום חמישי" כשהיום חמישי** = חמישי הבא.
3. **תאריך בלי שנה שעבר לפני יותר מ-60 יום** = השנה הבאה (למשל "עד 1.1" בספטמבר).
4. **מספר עם נקודה** (`17.9`) נחשב תאריך רק אחרי "עד", "ב-" או "ל-", כדי ש"2.5 מיליון" לא יהפוך לתאריך. עם לוכסן (`17/9`) הוא נחשב תאריך גם בלי מילת יחס.
5. **העברת אחריות לגורם אחר** הופכת משימה "לביצוע" ל"ממתין לתגובה", אלא אם נבחר סטטוס אחר.
6. **תזכורת ברירת מחדל** לפריט ממתין: יום אחד לפני המועד, ואחרי המועד כל יום.
7. **"לנדנד"** מוצג בתוך האפליקציה (תג בסרגל, מסך בוקר, רשימת ממתינים). התראות Push מתוכננות לשלב ב', כמו באפיון.
8. **"נקלטו ומחכות למיון"**: קליטה מהירה של טקסט בלבד (בלי תאריך, גורם, תגית או סימון). עריכה כלשהי בפרטי המשימה מוציאה אותה מהרשימה.
9. **שגרת ברירת מחדל** בהפעלה ראשונה: ארבעה פריטים מתוך הדוגמאות באפיון. אפשר לערוך ולמחוק.
10. **מחיקה** היא מחיקה רכה עם "ביטול". משימות שנמחקו נמחקות סופית אחרי 30 יום.
11. **הקלדה קולית** משתמשת בזיהוי הדיבור של הדפדפן (בכרום צריך רשת). בלי רשת, כפתור ההכתבה של המקלדת עובד כרגיל.

## ממצאים מבדיקה על אייפון אמיתי (שלב א')

- **גיליון תחתון קרס לגובה אפס בספארי**: `flex: 1` בעמודה בלי גובה מוגדר מתכווץ לאפס ב-WebKit (בכרום זה עבד). תוקן ל-`flex: 1 1 auto; min-height: 0`, והגובה המרבי מחושב מ-visualViewport כדי שהגיליון יישאר מעל המקלדת.
- **"הפעלת אחסון קבוע" לא עשתה כלום בספארי**: ספארי דוחה את הבקשה לאתר ומאפשר אחסון מוגן רק לאפליקציה שהותקנה למסך הבית. באייפון בדפדפן הכפתור הוסר ובמקומו הסבר; בשאר המקרים מוצגת תוצאה.

---

# שלב ב' — 14.9.2026

## תשובות המשתמש

| שאלה | תשובה |
|---|---|
| אחסון קוד | GitHub ציבורי: github.com/dudutal-dev/Peles, פריסה אוטומטית ל-GitHub Pages |
| טלפון | iPhone |
| תזכורות | Push אמיתי עם שרת קטן |

## ARD-006: ישיבות מחליפות את "פגישות היום"
- **הקשר**: בשלב א' היה יומן פנימי של פגישות (שעה ונושא). באפיון 3.2 ישיבה היא ישות עם משתתפים, הערות והתחייבויות.
- **החלטה**: ישות אחת, `Meeting`. פגישה ביומן היא ישיבה שעוד לא תועדה. שדרוג המסד (Dexie v2→v3) ממיר פגישות קיימות לישיבות, וגיבוי של שלב א' מומר בשחזור.
- **נימוק**: שני מושגים דומים ("פגישות" ו"ישיבות") היו מבלבלים. ההמרה נבדקה על מסד אמיתי בדפדפן ובבדיקת יחידה.

## ARD-007: קישור משימה לישיבה דרך המקור, לא ההקשר
- **החלטה**: התחייבות מישיבה נשמרת עם `source = meeting` (עם refId). שדה ההקשר נשאר פנוי לפרויקט.
- **נימוק**: משימה מישיבת סטטוס שייכת בדרך כלל גם לפרויקט. הקישור הדו-כיווני עובד דרך שני השדות.

## ARD-008: checklist של אירוע = משימות רגילות
- **החלטה**: כמו באפיון, כל פריט הוא Task עם `context = event`. התבניות לפי סוג מבוססות על הסקיל clalit-event-planner (מבנה אירוע, כשרות, נגישות, נשירה, שחרור מהעבודה).
- **תאריכי יעד**: כל פריט מקבל "X ימים לפני". כשהאירוע קרוב מהתבנית, הלו"ז **נדחס יחסית** (אחרת כל הפריטים נפלו על היום). שישי-שבת מוקדמים לחמישי. הזזת תאריך האירוע מזיזה את המשימות הפתוחות.
- **תקציב**: שלושת המודלים של הסקיל (לראש, כולל, משולב) עם תכנון ל-85% הגעה.

## ARD-009: גורמים נגזרים מהמשימות, בלי שדה קישור חדש
- **החלטה**: משימה שייכת לגורם אם שם האחראי או שם ההקשר תואם את שמו (השוואה סלחנית: גרשיים, נקודות, רווחים), או אם ההקשר מקושר אליו ב-refId. שמות שמופיעים במשימות ולא נשמרו מוצעים לשמירה.
- **נימוק**: אין מיגרציה של משימות קיימות, ו"מול קבלן X יש 3 דברים פתוחים" עובד מהרגע הראשון.

## ARD-010: התראות Push בלי תוכן בשרת
- **הקשר**: באייפון אין דרך אמינה להתריע כשהאפליקציה סגורה בלי Web Push, ו-Web Push דורש שרת.
- **החלטה**: שרת Cloudflare Worker קטן (`push-server/`, D1, cron כל 5 דקות). הטלפון מחשב **מתי** להעיר (בוקר ו/או ערב, רק בימים שיש על מה) ושולח לשרת רק זמנים וסוג. כשה-Push מגיע, ה-service worker קורא את IndexedDB ובונה את הטקסט.
- **השלכות**:
  - השרת לא רואה כותרות, שמות או תאריכים של משימות.
  - כל Push מציג התראה (iOS מבטל מנוי אחרי Push שקט).
  - מוצפן לפי RFC 8291 ונבדק מול וקטור הבדיקה הרשמי.
  - התראה עלולה להגיע באיחור של עד 5 דקות.
  - לוח הזמנים מחושב ל-30 יום ומתעדכן בכל שינוי. אם האפליקציה לא נפתחת חודש, התזכורות נעצרות.
- **ה-service worker** נכתב עכשיו ב-TypeScript (`src/sw/sw.ts`), נבנה בנפרד ומשתף את לוגיקת התצוגות עם האפליקציה.

## הנחות שלב ב'

1. **תזכורת בוקר** 07:30 ו**תזכורת ערב** 17:00, ימים א'–ה'. ניתנים לשינוי.
2. **ערב** = משימות שמועדן מחר ופריטים לנדנד. **בוקר** = איחורים, מועד היום, לנדנד, דחוף, ראש המינהל מחכה, ישיבות.
3. **ישיבה "מחכה לסיכום"** רק אם עברה, לא נסגרה, ויש בה תיעוד (משתתפים, הערות או התחייבויות). פגישת יומן בלי תיעוד לא מציקה.
4. **‎@אני ו-‎@ראש_המינהל** בשורת תיעוד מתפרשים כאחריות שלי / של ראש המינהל, לא כגורם חיצוני.
5. **מחיקת ישיבה** משאירה את המשימות שנולדו בה. **מחיקת אירוע** מוחקת גם את ה-checklist שלו (עם "ביטול" שמחזיר את שניהם).

## מה עוד לא נבדק

- התראות Push על אייפון אמיתי: השרת עוד לא נפרס (דורש חשבון Cloudflare של המשתמש).
- פתיחת מקלדת אוטומטית בקליטה מהירה ב-iOS אחרי התיקון.

## הצעד הבא (שלב ג' לפי האפיון)

דשבורד לראש המינהל (3.5), סנכרון בין מכשירים, ואינטגרציית Google Calendar (3.6).
